# --- Supabase (required, Block A) ---
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-KEEP-SECRET

# --- Claude API (Block D, chatbot + module generation — placeholder only) ---
ANTHROPIC_API_KEY=

# --- Voyage AI embeddings (Block D, retrieval — placeholder only) ---
VOYAGE_API_KEY=

# --- Resend (transactional email — placeholder only) ---
RESEND_API_KEY=

# --- Sentry (error tracking — placeholder only) ---
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=

# --- PostHog (analytics — placeholder only) ---
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
